# порт, на котором будет запускаться фронтенд
PORT=

# режим работы и сборки приложения (development, production)
MODE=

# URL API-сервера в виде http://localhost:[BACKEND_PORT], например http://localhost:8080,
# в скриптах у меня стоит 8080 порт, ставьте его
API_URL=
